eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost
spring:
  application:
    name: mailService
server:
  port: 8083
management:
  tracing:
    sampling:
      probability: '1.0'
